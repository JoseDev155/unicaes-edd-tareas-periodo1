// Array
let numeros = [5, 12, 8, 130, 44, 7, 19];

// Crear un nuevo array solo con los números pares y muéstralo en consola
let numerosPares = numeros.filter(num => num % 2 === 0);
console.log(numerosPares);